apiVersion: v1
kind: Service
metadata:
  name: arhitect-cloud-project-deployment
spec:
  type: LoadBalancer
  selector:
    app: arhitect-cloud-project
  ports:
    - port: 5050
      targetPort: 8080